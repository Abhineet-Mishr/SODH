# SODH Literature Toolkit Frontend

This React + TypeScript app provides the literature conversion and deduplication UI.

## Run

```bash
npm install
npm run dev
```

The dev server proxies `/api` to `http://localhost:8000`.

